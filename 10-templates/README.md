# 10-templates — reusable starting points

Blueprints for anything you create more than twice: documents, workflows,
project scaffolds. Starting from a template beats starting from memory —
templates encode your standards.
