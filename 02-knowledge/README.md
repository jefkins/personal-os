# 02-knowledge — what the AI should know

Domain knowledge your AI draws on before producing anything domain-specific.
Organise by domain: `02-knowledge/<domain>/<topic>.md`.

Keep an `_index.md` listing what exists — agents read the index first and load
only what the task needs (see context-router).
