# Profile: build a script or tool

**Always load:** `OS.md` §2 (WAT rules), the workflow that needs this tool
**Load if depth needed:** an existing similar script in `07-scripts/`
**Relevant scripts:** everything in `07-scripts/` — check for reuse FIRST
**Notes:** New script domain → add a row to OS.md's map (parity rule).
Scripts read config from `.env`, never hardcoded.
