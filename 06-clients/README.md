# 06-clients — per-client workspaces

One folder per client. **This folder is gitignored except this README** —
client data never commits to a repo, even a private one you might someday
make public. Keep durable client files in your cloud drive.
