# 12-memory legacy — archived

These files held **personal** memory (strategy north-star, personal ideas) that
belongs in the agent's memory tool + the notes vault per the 12-memory routing
rule (`12-memory/README.md`). Archived, not deleted — history is preserved.

- `strategy.md` — the personal north star. If it needs to live on, port the
  "what winning looks like" lines into the vault and/or the agent's user profile.
- `ideas_vault.md` — empty template stub; ideas route to the vault's
  ideas-brainstorming folder.
